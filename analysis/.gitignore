# 仮想環境(venv)はマシンごとに作り直すものなのでコミットしない
.venv/

# 分析対象としてcloneした実際のGitリポジトリはコミットしない
# (サイズが大きい上、いつでも再cloneできるため)
repos/

# git logから生成した実データ(CSV/ログ)もコミットしない
# (再現性のあるスクリプトさえあれば誰でも再生成できるため)
data/

# Pythonの実行時に生成されるキャッシュ類
__pycache__/
*.pyc

# エディタ/OSが生成する不要ファイル
.DS_Store
.ipynb_checkpoints/
